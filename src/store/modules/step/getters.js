const STEP_NUMBER = state => state.STEP_NUMBER
const JSON_FILE = state => state.JSON_FILE
const JSON_TABLE = state => state.JSON_TABLE
const WORD_CLOUD = state => state.WORD_CLOUD
const READY = state => state.READY
const UUID = state => state.UUID
const BUTTON_NAME = state => state.BUTTON_NAME

export default {
  STEP_NUMBER,
  JSON_FILE,
  JSON_TABLE,
  WORD_CLOUD,
  READY,
  UUID,
  BUTTON_NAME
};
